{
	// Don't make changes to this file directly as they can get wiped out when the
	// plugin is updated. Instead transfer what you need to the 'Preferences - Package Settings - ITDCHelper - Settings User' file.

	// API KEY for project service
	"api_key": ""
}
